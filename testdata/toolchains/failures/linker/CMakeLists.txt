cmake_minimum_required(VERSION 3.31)
project(unit_test_ide_linker_failure LANGUAGES CXX)
if(MSVC)
  add_executable(linker_failure src/main.cpp)
  target_link_options(linker_failure PRIVATE "/unknown-native_missing_symbol")
else()
  add_executable(linker_failure src/main.cpp)
endif()
